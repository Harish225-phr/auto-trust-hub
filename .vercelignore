# Files Vercel should NOT upload (they're regenerated by the build).
node_modules
.git
.cache
.vscode
.idea
mem
knowledge
/tmp
# Keep dist/ — Vercel needs dist/client (output) and dist/server (function asset)
